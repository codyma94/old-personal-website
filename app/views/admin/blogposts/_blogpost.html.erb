<div class="post" id="<%= blogpost.id %>">
  <div class="blogpost-header">
    <div class="blogpost-left blogpost-title">
      <h3>
        <%= blogpost.title %>
      </h3>
    </div>
    <div class="blogpost-right blogpost-date">
      <p>
        <%= blogpost.created_at.to_time.strftime('%B %e, %Y') %>
      </p>
    </div>
  </div>
  <div class="blogpost-content">
    <%= markdown(blogpost.content) %>
  </div>
</div>

  <%= link_to "Delete", admin_blogpost_path(blogpost), method: "delete", data: { confirm: "You sure?" }%>
</div>
